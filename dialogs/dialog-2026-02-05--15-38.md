## Me:

```
请解释 #file:base_test.cc 中的这两个测试都测试了什么。请用given-when-then格式解释，以便我理解。
```

## vscode with github copilot with auto:

```
根据 base_test.cc 中的代码，这两个测试使用 given-when-then 格式解释如下：

## 测试1：`ReadUndefinedVariableWithDefaultValue`

**Given** (给定)：  
环境变量 `.env.example` 已加载，且尝试读取一个未定义的变量 `UNDEFINED_VAR`

**When** (当)：  
调用 `dotenv::getenv("UNDEFINED_VAR", "EHLO")` 方法，传入默认值 `"EHLO"`

**Then** (那么)：  
应该返回提供的默认值 `"EHLO"`

---

## 测试2：`ReadDefinedVariableWithDefaultValue`

**Given** (给定)：  
环境变量 `.env.example` 已加载，且 `DEFINED_VAR` 在该文件中已定义且值为 `"OLHE"`

**When** (当)：  
调用 `dotenv::getenv("DEFINED_VAR", "EHLO")` 方法，同样传入默认值 `"EHLO"`

**Then** (那么)：  
应该返回实际定义的值 `"OLHE"`，而不是默认值

---

**总结**：这两个测试验证了 `dotenv::getenv()` 函数的完整行为——当变量未定义时使用默认值，当变量已定义时返回实际值。
```